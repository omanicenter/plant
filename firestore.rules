rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // أي مستخدم مسجّل دخوله يقرأ ويكتب
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
